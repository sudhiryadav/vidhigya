export interface Invoice { id: string; caseId: string; clientId: string; lawyerId: string; amount: number; currency: string; status: InvoiceStatus; dueDate: Date; items: InvoiceItem[]; createdAt: Date; updatedAt: Date; } export enum InvoiceStatus { DRAFT = "draft", SENT = "sent", PAID = "paid", OVERDUE = "overdue", CANCELLED = "cancelled" } export interface InvoiceItem { id: string; description: string; quantity: number; rate: number; amount: number; }
