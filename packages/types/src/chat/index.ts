export interface ChatMessage { id: string; senderId: string; receiverId: string; caseId?: string; content: string; type: MessageType; attachments?: ChatAttachment[]; read: boolean; createdAt: Date; } export enum MessageType { TEXT = "text", FILE = "file", IMAGE = "image", VOICE = "voice" } export interface ChatAttachment { id: string; fileName: string; fileType: string; fileSize: number; url: string; }
